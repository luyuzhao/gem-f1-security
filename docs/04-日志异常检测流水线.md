# 系统日志异常检测流水线

```mermaid
flowchart LR
    A["📝 原始日志文本"] --> B["🔧 拼写纠错<br/>~400条规则"]
    B --> C["🔍 关键短语匹配<br/>10类异常短语库"]
    C --> D["📍 标记聚类<br/>离散命中→连续区域"]
    D --> E["📐 边界偏移学习<br/>训练集标注监督"]
    E --> F["🌲 LightGBM 结构辅助<br/>行级事件密度/时间戳"]
    F --> G["↔️ 上下文窗口扩展<br/>前后捕获关联行"]
    G --> H["↩️ Foldback 精修<br/>弱证据回缩边界"]
    H --> I["📏 长度风格对齐<br/>训练集均值匹配"]
    I --> J["📤 输出异常区间<br/>[start, end] × 异常类型"]
```
